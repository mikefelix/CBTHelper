<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mozzarelly.cbthelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".CBTApplication"
        android:theme="@style/AppTheme.NoActionBar">

        <activity
            android:name=".viewentries.ViewEntriesActivity"
            android:label="@string/title_activity_view_entries"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".emotionhelp.EmotionHelpActivity"
            android:label="@string/title_activity_view_entries"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".editentry.AddEntryActivity"
            android:label="@string/title_activity_add_entry"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".analyze.AnalyzeActivity"
            android:label="@string/title_activity_analyze_entry"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".cogvalid.CogValidActivity"
            android:label="@string/title_cogvalid_activity"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".replacement.ReplacementThoughtsActivity"
            android:label="@string/title_ratrep_activity"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity
            android:name=".behavior.BehaviorActivity"
            android:label="@string/title_ratrep_activity"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>